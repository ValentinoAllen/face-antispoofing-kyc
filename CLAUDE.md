# Working in this repository

- Before any work, read `docs/RULES.md`, especially §6 "Constraints for AI assistants". Those rules
  are binding.
- Read `docs/PROGRESS.md` for the current status and open questions.
- Never add attribution trailers to commits, never invent numbers, and never touch `data/`.
- Run `/session-end` before ending a working session.
